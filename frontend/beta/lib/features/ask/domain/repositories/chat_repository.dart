// TODO: Uncomment when backend is ready
/*
import 'package:dartz/dartz.dart';
import '../entities/chat_entry.dart';

abstract class ChatRepository {
  Future<Either<Exception, ChatEntry>> askQuestion(ChatRequest request);
  Future<Either<Exception, List<ChatEntry>>> getChatHistory(String userId);
  Future<Either<Exception, void>> toggleFavorite(String entryId);
  Future<Either<Exception, List<ChatEntry>>> getFavorites(String userId);
}
*/ 