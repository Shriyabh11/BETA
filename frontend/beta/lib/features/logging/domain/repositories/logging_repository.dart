import 'package:dartz/dartz.dart';
import '../entities/log_entry.dart';
/*
abstract class LoggingRepository {
  Future<Either<Exception, LogEntry>> submitLog(LogRequest request);
  Future<Either<Exception, List<LogEntry>>> getLogs(String userId);
  Future<Either<Exception, Map<String, dynamic>>> extractData(String text);
} 
*/
